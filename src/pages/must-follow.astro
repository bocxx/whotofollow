---
import RoundupPage from "../components/RoundupPage.astro";
import type { Locale } from "../i18n/messages";
const locale = (Astro.currentLocale ?? "nl") as Locale;
---
<RoundupPage slug="must-follow" locale={locale} />
